@page
@model EsafyCapcitacion.Pages.Options.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuestionOption[0].QuestionText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuestionOption[0].IsCorrectAns)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.QuestionOption) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsCorrectAns)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.QuestionOptionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.QuestionOptionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.QuestionOptionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
