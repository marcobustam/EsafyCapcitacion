@page
@model EsafyCapcitacion.Pages.Exams.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Examination[0].CourseId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Examination[0].CourseTestId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Examination[0].StudentId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Examination) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CourseId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseTestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ExaminationId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ExaminationId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ExaminationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
