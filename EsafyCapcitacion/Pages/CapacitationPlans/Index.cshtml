@page
@model EsafyCapcitacion.Pages.CapacitationPlans.IndexModel

@{
    ViewData["Title"] = "Index";
}
<div class="card card-body">
    <div class="card card-header">
        <div class="row">
            <div class="col-11">
                <h4>
                    Plan de Capacitación
                </h4>
            </div>
            <div class="col-1">
                <a asp-page="Create" class="btn btn-sm btn-success float-right">
                    <i class="fa fa-plus-circle"></i>
                </a>
            </div>
        </div>
    </div>
    <hr />


    <hr />
    <table class="table table-hover table-bordered border-primary" id="datata">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.CapacitationPlan[0].CompanyId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CapacitationPlan[0].PlanName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CapacitationPlan[0].IsActive)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CapacitationPlan[0].Created)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CapacitationPlan[0].Modified)
                </th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.CapacitationPlan)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CompanyId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PlanName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IsActive)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Created)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Modified)
                    </td>
                    <td>
                        <div class="btn-group">
                            <a asp-page="./Edit" class="btn btn-sm btn-warning" asp-route-id="@item.CapacitationPlanId"><i class="fa fa-edit"></i></a>
                            <a asp-page="./Details" class="btn btn-sm btn-info" asp-route-id="@item.CapacitationPlanId"><i class="fa fa-eye"></i></a>
                            <a asp-page="./Delete" class="btn btn-sm btn-danger" asp-route-id="@item.CapacitationPlanId"><i class="fa fa-times"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr class="bg-light">
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            </tr>
        </tfoot>
    </table>
    <hr />
</div>

@* ******************************************************* *@
